# SP-2 — Schema CV v2: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng schema CV v2 cạnh v1, kèm bộ chuyển đổi hai chiều đã chứng minh không mất dữ liệu, để SP-3/SP-4 có nền dữ liệu thật mà `apps/web` vẫn chạy nguyên vẹn tới SP-5.

**Architecture:** v2 **sống song song** v1, không thay thế. `packages/schema/src/profile.ts` (v1) không bị đụng tới; v2 nằm ở file mới. Migration 010 chỉ thêm cột `data_v2`, không sửa `data`. Backend Go nhận diện phiên bản bằng cách nhìn dữ liệu (`sections` có mặt ⇒ v2), nên một binary phục vụ được cả hai hình dạng trong suốt giai đoạn chuyển tiếp.

**Tech Stack:** TypeScript + Zod (schema, converter), PostgreSQL 16 + jsonb (lưu trữ), Go 1.x (API + worker), Vitest (test TS), `go test` (test Go), tsx (script migration/backfill).

---

## Quyết định nền: vì sao v2 không thay thế v1

Spec §2 viết "`packages/schema` được viết lại". Đọc thẳng như vậy sẽ hỏng:

`grep -rl "ProfileSchema\|@hr/schema" frontend --include=*.ts --include=*.tsx` cho **hơn 70 file**, phần lớn nằm trong `apps/web` — bản Next đang phục vụ production ở cổng 3000 và phải sống tới SP-5. Viết lại tại chỗ là làm chết nó ngay lập tức.

Ba hệ quả bắt buộc, ràng buộc mọi task bên dưới:

1. **Không sửa `packages/schema/src/profile.ts`.** v2 nằm ở `cv.ts` mới. `index.ts` export cả hai.
2. **Migration 010 chỉ thêm.** Spec §2.4 tự viết "thêm cột/ràng buộc, không xoá gì ở bước này". Backfill ghi vào `data_v2`; `data` giữ nguyên byte-for-byte. `hr-web` không nhận ra có gì thay đổi.
3. **Go phục vụ cả hai hình dạng.** Không đổi prompt sang v2 vô điều kiện — chọn theo hình dạng hồ sơ thật. Nhánh v1 bị xoá ở SP-5, không sớm hơn.

Kết quả cuối SP-2: dữ liệu v2 tồn tại và đúng, chuyển được hai chiều, chưa ai đọc nó trong production. SP-3 là nơi đầu tiên đọc.

## Global Constraints

- **Không sửa** `frontend/packages/schema/src/profile.ts`, `frontend/apps/web/**`. Đụng vào là sai task.
- `PII_PATHS_V2` phải khai đúng năm đường dẫn spec §2.2(2) liệt kê: `/sections/intro/fullName`, `/sections/intro/email`, `/sections/intro/phone`, `/sections/intro/location`, `/sections/intro/avatarUrl`. Thiếu một dòng là PII đi thẳng ra cloud, **không có lỗi nào được ném ra**.
- Backfill **idempotent**: chạy `n` lần cho cùng kết quả như chạy một lần.
- Mọi field v1 không có chỗ ở v2 — cả mảng `basics.links`, `basics.dob`, `work[].type`, `skills[].level`, `skills[].group`, `projects[].tech` — phải được ghi vào `_meta.originalLinks` / `_meta.droppedFields`, không được vứt. Đây là điều kiện để đường lùi khôi phục nguyên trạng.
- **Cả khoá `ProfileSchema` không biết cũng phải giữ.** `.parse` của Zod lược khoá lạ trong im lặng. Dữ liệu thật có `basics.summary` sót lại từ trước migration 009, và nó biến mất không dấu vết. Backfill phải so với JSON **thô** lấy từ DB chứ không phải bản đã parse — so với bản đã parse là tự bịt mắt, vì khoá bị lược đã biến khỏi cả hai vế trước khi so.
- **Không diễn tập rollback trên dữ liệu thật.** Bằng chứng đường lùi là script kiểm tra chỉ đọc. Xem Task 4 Step 6b.
- **Không mã hoá dữ liệu vào văn xuôi.** Dòng `Công nghệ: …` ở `highlights[0]` chỉ để hiển thị; nguồn sự thật là mảng trong `_meta`. Bóc ngược từ chuỗi thì một bullet do người dùng tự viết mở đầu bằng đúng mấy chữ đó cũng bị nuốt.
- `_meta.verified` phải **dịch cả khoá JSON Pointer**, không copy nguyên. Riêng `skills` phải **tính lại chỉ số** (v1 phẳng → v2 gom nhóm), không phải đổi tên.
- `skills[].canonical` phải giữ trong `_meta.canonical`: lớp đối chiếu JD vẫn dùng nó (BR-57.1). Mất nó là điểm khớp JD đổi mà không ai biết vì sao.
- Mọi test viết trước code. Xem `superpowers:test-driven-development`.
- Commit sau mỗi task, message tiếng Việt, kèm `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Chạy được: `cd frontend && npx vitest run --project unit` và `cd backend && go test ./...` phải xanh sau **mỗi** task.

---

## Cấu trúc file

| File | Trách nhiệm |
|---|---|
| `frontend/packages/schema/src/cv.ts` | **Tạo.** Kiểu CV v2 + `PII_PATHS_V2`. Chỉ khai hình dạng, không chứa logic chuyển đổi. |
| `frontend/packages/schema/src/cv-migrate.ts` | **Tạo.** `profileToCV()` và `cvToProfile()`. Tách khỏi `cv.ts` vì hai file đổi vì lý do khác nhau: hình dạng đổi hiếm, quy tắc ánh xạ đổi thường. |
| `frontend/packages/schema/test/cv.test.ts` | **Tạo.** Hình dạng v2 và danh sách PII. |
| `frontend/packages/schema/test/cv-migrate.test.ts` | **Tạo.** Ánh xạ từng field + khứ hồi golden. |
| `frontend/packages/schema/test/fixtures/profile-v1-*.json` | **Tạo.** Ba hồ sơ thật đã ẩn danh. |
| `frontend/packages/schema/src/index.ts` | **Sửa.** Export thêm `cv.js`, `cv-migrate.js`. |
| `backend/db/migrations/010_cv_schema_v2.sql` | **Tạo.** Thêm cột, không xoá gì. |
| `backend/db/backfill-v2.ts` | **Tạo.** Backfill hai chiều, idempotent, có `--dry-run`. |
| `backend/internal/pii/pii.go` | **Sửa.** Che được cả hai hình dạng. |
| `backend/internal/pii/pii_test.go` | **Tạo.** Package này chưa có test. |
| `backend/cmd/worker/matching.go` | **Sửa.** `profileChunks` đọc được `sections`. |
| `backend/internal/api/server.go` | **Sửa.** Prompt và validate chọn theo phiên bản. |
| `frontend/apps/web-spa/src/types.ts` | **Sửa.** `description: string` → `highlights: string[]`. |
| `frontend/apps/web-spa/src/components/CVEditorView.tsx` | **Sửa.** Textarea → danh sách bullet sửa từng dòng. |

---

### Task 1: Kiểu CV v2 và danh sách PII

**Files:**
- Create: `frontend/packages/schema/src/cv.ts`
- Modify: `frontend/packages/schema/src/index.ts`
- Test: `frontend/packages/schema/test/cv.test.ts`

**Interfaces:**
- Consumes: `zod` (đã có trong workspace).
- Produces: `CVSchema`, `CVSectionsSchema`, `IntroSectionSchema`, `ExperienceItemSchema`, `ProjectItemSchema`, `EducationItemSchema`, `SkillItemSchema`, `ActivityItemSchema`, `CertificationItemSchema`, `LanguageItemSchema`, `CVDesignSchema`, `CVMetaSchema`, `PII_PATHS_V2`, kiểu `CV`. Task 2 và 3 dùng `CVSchema.parse`.

- [ ] **Step 1: Viết test thất bại**

Tạo `frontend/packages/schema/test/cv.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { CVSchema, PII_PATHS_V2 } from '../src/cv.js'

const minimal = {
  schemaVersion: 2,
  id: 'cv-1',
  title: 'CV Backend',
  lastModified: '2026-08-09T10:00:00Z',
  language: 'vi',
  sections: {
    intro: { fullName: 'Ada', title: 'Kỹ sư', email: '', phone: '', location: '', summary: '' },
    experience: [], projects: [], education: [],
    skills: [], activities: [], certifications: [], languages: [],
  },
  design: { template: 'modern', accentColor: '#4F46E5', font: 'Roboto', fontSize: 14, spacing: 'normal' },
  activeSections: {
    intro: true, experience: true, projects: true, education: true,
    skills: true, activities: true, certifications: true, languages: true,
  },
  _meta: { verified: {}, source: 'manual' },
}

describe('CV v2', () => {
  it('nhận hồ sơ tối thiểu hợp lệ', () => {
    expect(CVSchema.parse(minimal).schemaVersion).toBe(2)
  })

  it('kinh nghiệm dùng highlights[] chứ không phải description', () => {
    const cv = CVSchema.parse({
      ...minimal,
      sections: {
        ...minimal.sections,
        experience: [{
          id: 'e1', title: 'Engineer', company: 'FPT',
          startDate: '2023-01', endDate: '', current: true,
          highlights: ['Giảm 40% độ trễ', 'Dựng pipeline CI'],
        }],
      },
    })
    expect(cv.sections.experience[0]!.highlights).toHaveLength(2)
  })

  it('từ chối description ở kinh nghiệm — chat sinh patch nhắm vào từng bullet', () => {
    expect(() => CVSchema.parse({
      ...minimal,
      sections: {
        ...minimal.sections,
        experience: [{
          id: 'e1', title: 'Engineer', company: 'FPT',
          startDate: '2023-01', endDate: '', current: true,
          description: 'Làm nhiều thứ',
        }],
      },
    })).toThrow()
  })

  it('khai đúng năm đường dẫn PII của v2', () => {
    expect([...PII_PATHS_V2]).toEqual([
      '/sections/intro/fullName',
      '/sections/intro/email',
      '/sections/intro/phone',
      '/sections/intro/location',
      '/sections/intro/avatarUrl',
    ])
  })
})
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv.test.ts
```

Kỳ vọng: FAIL — `Cannot find module '../src/cv.js'`.

- [ ] **Step 3: Viết `cv.ts`**

```ts
import { z } from 'zod'

/**
 * CV v2 — spec 2026-08-09 §2.1.
 *
 * Sống CẠNH ProfileSchema v1 trong profile.ts, không thay thế. `apps/web` có
 * hơn 70 file bám vào v1 và phải chạy tới SP-5; xoá v1 bây giờ là làm chết bản
 * đang phục vụ production.
 */

export const CVLanguageSchema = z.enum(['vi', 'en'])

/**
 * PII v2 — năm đường dẫn này KHÔNG BAO GIỜ được gửi tới model.
 *
 * `redact_pii.required_local: true` trong config.yml bám vào đúng danh sách
 * này. Thiếu một dòng thì PII đi thẳng tới provider cloud và không có lỗi nào
 * được ném ra — hỏng im lặng, loại tệ nhất. Bản Go tương ứng nằm ở
 * backend/internal/pii/pii.go và phải khớp từng dòng.
 *
 * `website` và `summary` KHÔNG phải PII: chúng là nội dung nghề nghiệp, model
 * cần đọc để đề xuất có nghĩa.
 */
export const PII_PATHS_V2 = [
  '/sections/intro/fullName',
  '/sections/intro/email',
  '/sections/intro/phone',
  '/sections/intro/location',
  '/sections/intro/avatarUrl',
] as const

export const IntroSectionSchema = z.object({
  fullName: z.string(),
  title: z.string().default(''),
  email: z.string().default(''),
  phone: z.string().default(''),
  location: z.string().default(''),
  website: z.string().optional(),
  summary: z.string().default(''),
  avatarUrl: z.string().optional(),
})

/**
 * `.strict()` ở mọi mục có bullet, và bullet là `highlights: string[]`.
 *
 * Spec §2.2(3): chat sinh JSON Patch nhắm vào một gạch đầu dòng cụ thể
 * (`/sections/experience/0/highlights/2`). Nếu đây là một chuỗi `description`,
 * mọi đề xuất của AI biến thành ghi đè nguyên khối và màn duyệt diff không còn
 * gì đáng duyệt. `.strict()` để `description` bị từ chối thẳng thay vì lặng lẽ
 * bị bỏ qua rồi mất dữ liệu.
 */
export const ExperienceItemSchema = z
  .object({
    id: z.string(),
    title: z.string(),
    company: z.string(),
    startDate: z.string().default(''),
    endDate: z.string().default(''),
    current: z.boolean().default(false),
    highlights: z.array(z.string()).default([]),
  })
  .strict()

export const ProjectItemSchema = z
  .object({
    id: z.string(),
    name: z.string(),
    role: z.string().default(''),
    startDate: z.string().default(''),
    endDate: z.string().default(''),
    link: z.string().optional(),
    highlights: z.array(z.string()).default([]),
  })
  .strict()

export const EducationItemSchema = z
  .object({
    id: z.string(),
    school: z.string(),
    degree: z.string().default(''),
    fieldOfStudy: z.string().default(''),
    startDate: z.string().default(''),
    endDate: z.string().default(''),
    gpa: z.string().optional(),
    highlights: z.array(z.string()).default([]),
  })
  .strict()

/** `skills` v2 gom theo nhóm: một dòng là một mảng kỹ năng cùng category (UC-57). */
export const SkillItemSchema = z
  .object({
    id: z.string(),
    category: z.string(),
    skills: z.array(z.string()).default([]),
  })
  .strict()

export const ActivityItemSchema = z
  .object({
    id: z.string(),
    organization: z.string(),
    role: z.string().default(''),
    startDate: z.string().default(''),
    endDate: z.string().default(''),
    highlights: z.array(z.string()).default([]),
  })
  .strict()

export const CertificationItemSchema = z
  .object({
    id: z.string(),
    name: z.string(),
    issuer: z.string().default(''),
    date: z.string().default(''),
    link: z.string().optional(),
  })
  .strict()

export const LanguageItemSchema = z
  .object({
    id: z.string(),
    language: z.string(),
    proficiency: z.string().default(''),
  })
  .strict()

export const CVSectionsSchema = z.object({
  intro: IntroSectionSchema,
  experience: z.array(ExperienceItemSchema).default([]),
  projects: z.array(ProjectItemSchema).default([]),
  education: z.array(EducationItemSchema).default([]),
  skills: z.array(SkillItemSchema).default([]),
  activities: z.array(ActivityItemSchema).default([]),
  certifications: z.array(CertificationItemSchema).default([]),
  languages: z.array(LanguageItemSchema).default([]),
})

export const CVDesignSchema = z.object({
  template: z.enum(['modern', 'classic', 'professional']).default('modern'),
  accentColor: z.string().default('#4F46E5'),
  font: z.enum(['Roboto', 'Open Sans', 'Lato']).default('Roboto'),
  fontSize: z.number().default(14),
  spacing: z.enum(['condensed', 'normal', 'wide']).default('normal'),
})

export const ActiveSectionsSchema = z.object({
  intro: z.boolean().default(true),
  experience: z.boolean().default(true),
  projects: z.boolean().default(true),
  education: z.boolean().default(true),
  skills: z.boolean().default(true),
  activities: z.boolean().default(true),
  certifications: z.boolean().default(true),
  languages: z.boolean().default(true),
})

/**
 * `verified` — xương sống chống bịa, giữ nguyên ngữ nghĩa từ v1: mọi nội dung
 * do AI sinh ra là false cho tới khi người dùng xác nhận (UC-22, UC-53).
 *
 * `originalLinks` / `droppedFields` / `canonical` là kho chứa những gì v2 không
 * hiển thị nhưng không được phép mất: thiếu chúng thì đường lùi v2→v1 không
 * khôi phục được nguyên trạng, và `canonical` mất là điểm khớp JD đổi âm thầm
 * (BR-57.1).
 */
export const CVMetaSchema = z.object({
  verified: z.record(z.boolean()).default({}),
  source: z.enum(['manual', 'pdf_import', 'ai_generated']).default('manual'),
  /**
   * TOÀN BỘ `basics.links` của v1, không phải phần dư.
   *
   * v2 chỉ có một `website: string`, nên nhãn của link đầu tiên cũng không có
   * chỗ đứng — giữ lại `links[1..]` thôi thì khứ hồi dựng lại nhãn bằng cách
   * đoán, và đoán sai. Giữ cả mảng thì `website` chỉ là bản rút gọn để hiển
   * thị, còn nguồn sự thật vẫn đầy đủ.
   */
  originalLinks: z.array(z.object({ label: z.string(), url: z.string() })).default([]),
  /** Field v1 không có chỗ ở v2, khoá là JSON Pointer của v1. */
  droppedFields: z.record(z.string()).default({}),
  canonical: z.record(z.string()).default({}),
})

export const CVSchema = z.object({
  schemaVersion: z.literal(2),
  id: z.string(),
  title: z.string(),
  lastModified: z.string(),
  language: CVLanguageSchema,
  sections: CVSectionsSchema,
  design: CVDesignSchema.default({}),
  activeSections: ActiveSectionsSchema.default({}),
  _meta: CVMetaSchema.default({}),
})

export type CV = z.infer<typeof CVSchema>
export type CVSections = z.infer<typeof CVSectionsSchema>
export type IntroSection = z.infer<typeof IntroSectionSchema>
export type ExperienceItem = z.infer<typeof ExperienceItemSchema>
export type SkillItem = z.infer<typeof SkillItemSchema>
```

- [ ] **Step 4: Export từ `index.ts`**

Sửa `frontend/packages/schema/src/index.ts` thành:

```ts
export * from './profile.js'
export * from './cv.js'
export * from './jd.js'
export * from './patch.js'
export * from './kb.js'
export * from './review.js'
```

- [ ] **Step 5: Chạy test, phải xanh**

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv.test.ts
```

Kỳ vọng: PASS 4/4.

Rồi kiểm không làm hỏng gì:

```bash
cd frontend && npm run typecheck && npx vitest run --project unit --project ui
```

Kỳ vọng: xanh toàn bộ. `PII_PATHS` v1 và `PII_PATHS_V2` cùng export từ `index.ts`, tên khác nhau nên không đụng nhau.

- [ ] **Step 6: Commit**

```bash
git add frontend/packages/schema/src/cv.ts frontend/packages/schema/src/index.ts \
        frontend/packages/schema/test/cv.test.ts
git commit -m "feat(schema): kiểu CV v2 sống cạnh ProfileSchema v1

v2 nằm ở file mới chứ không viết đè profile.ts: hơn 70 file trong
apps/web bám vào v1 và bản đó phải phục vụ production tới SP-5.

Mục có bullet dùng highlights[] và .strict(), nên description bị từ chối
thẳng. Chat sinh patch nhắm vào từng gạch đầu dòng; để nguyên một chuỗi
thì mọi đề xuất thành ghi đè nguyên khối và màn duyệt diff vô nghĩa.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Chuyển đổi v1 → v2

**Files:**
- Create: `frontend/packages/schema/src/cv-migrate.ts`
- Create: `frontend/packages/schema/test/cv-migrate.test.ts`
- Modify: `frontend/packages/schema/src/index.ts`

**Interfaces:**
- Consumes: `CVSchema`, kiểu `CV` (Task 1); `ProfileSchema`, kiểu `Profile` (v1, có sẵn).
- Produces: `profileToCV(profile: Profile, meta: { id: string; title: string; lastModified: string }): CV`. Task 3 dùng để dựng test khứ hồi; Task 4 gọi từ script backfill.

- [ ] **Step 1: Viết test thất bại**

Tạo `frontend/packages/schema/test/cv-migrate.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { ProfileSchema } from '../src/profile.js'
import { profileToCV } from '../src/cv-migrate.js'

const META = { id: 'cv-1', title: 'CV của tôi', lastModified: '2026-08-09T10:00:00Z' }

const v1 = ProfileSchema.parse({
  schemaVersion: 1,
  language: 'vi',
  basics: {
    name: 'Nguyễn Văn A',
    headline: 'Kỹ sư AI',
    introduce: 'Ba năm làm edge AI',
    email: 'a@example.com',
    phone: '0901234567',
    location: 'Hà Nội',
    dob: '1999-01-02',
    photo: 'https://cdn.example/a.jpg',
    links: [
      { label: 'GitHub', url: 'https://github.com/a' },
      { label: 'Blog', url: 'https://a.dev' },
    ],
  },
  work: [{ org: 'FPT', role: 'Engineer', type: 'fulltime', startDate: '2023-01', highlights: ['Giảm 40% độ trễ'] }],
  projects: [{ name: 'Cân AI', tech: ['Go', 'ONNX'], url: 'https://x.dev', highlights: ['Chạy trên Jetson'] }],
  education: [{ school: 'HCMUTE', degree: 'Kỹ sư', major: 'Cơ điện tử' }],
  skills: [
    { name: 'Python', group: 'Programming', canonical: 'python', level: 'advanced' },
    { name: 'Go', group: 'Programming', canonical: 'golang' },
    { name: 'Docker', group: 'MLOps', canonical: 'docker' },
  ],
  _meta: { verified: { '/basics/name': true, '/work/0/highlights/0': true }, source: 'pdf_import' },
})

describe('profileToCV', () => {
  it('đổi tên field của intro', () => {
    const intro = profileToCV(v1, META).sections.intro
    expect(intro.fullName).toBe('Nguyễn Văn A')
    expect(intro.title).toBe('Kỹ sư AI')
    expect(intro.summary).toBe('Ba năm làm edge AI')
    expect(intro.avatarUrl).toBe('https://cdn.example/a.jpg')
  })

  it('lấy link đầu làm website và giữ NGUYÊN CẢ MẢNG trong _meta', () => {
    const cv = profileToCV(v1, META)
    expect(cv.sections.intro.website).toBe('https://github.com/a')
    // Cả mảng, không phải phần dư: nhãn 'GitHub' của link đầu cũng không có chỗ
    // ở v2, giữ thiếu là khứ hồi phải đoán nhãn và đoán sai.
    expect(cv._meta.originalLinks).toEqual([
      { label: 'GitHub', url: 'https://github.com/a' },
      { label: 'Blog', url: 'https://a.dev' },
    ])
  })

  it('cất mọi field v1 không có chỗ ở v2 vào droppedFields, khoá là JSON Pointer', () => {
    const dropped = profileToCV(v1, META)._meta.droppedFields
    expect(dropped['/basics/dob']).toBe('1999-01-02')
    // work[].type và skills[].level cũng không có chỗ ở v2. Bỏ sót chúng thì
    // test khứ hồi ở Task 3 đỏ, và đó là chỗ duy nhất phát hiện ra.
    expect(dropped['/work/0/type']).toBe('fulltime')
    expect(dropped['/skills/0/level']).toBe('advanced')
  })

  it('gom skills theo group và giữ canonical trong _meta', () => {
    const cv = profileToCV(v1, META)
    expect(cv.sections.skills.map((s) => s.category)).toEqual(['Programming', 'MLOps'])
    expect(cv.sections.skills[0]!.skills).toEqual(['Python', 'Go'])
    expect(cv._meta.canonical).toEqual({ Python: 'python', Go: 'golang', Docker: 'docker' })
  })

  it('gộp tech của dự án vào bullet đầu tiên', () => {
    expect(profileToCV(v1, META).sections.projects[0]!.highlights[0]).toBe('Công nghệ: Go, ONNX')
  })

  it('DỊCH khoá JSON Pointer của _meta.verified, không copy', () => {
    const verified = profileToCV(v1, META)._meta.verified
    expect(verified['/sections/intro/fullName']).toBe(true)
    expect(verified['/sections/experience/0/highlights/0']).toBe(true)
    expect(verified['/basics/name']).toBeUndefined()
  })

  it('TÍNH LẠI chỉ số cho con trỏ kỹ năng, không đổi tên', () => {
    // Docker là kỹ năng thứ 2 (phẳng) của v1, nhưng sau khi gom nhóm nó là
    // phần tử đầu của nhóm 'MLOps'. Đổi tên suông thì con trỏ trỏ nhầm sang
    // Python và dấu đã-xác-nhận nằm lên đúng một kỹ năng khác.
    const withSkill = ProfileSchema.parse({
      ...v1,
      _meta: { ...v1._meta, verified: { '/skills/2': true } },
    })
    const verified = profileToCV(withSkill, META)._meta.verified
    expect(verified['/sections/skills/1/skills/0']).toBe(true)
    expect(verified['/sections/skills/0/skills/0']).toBeUndefined()
  })

  it('giữ tech thành mảng thật trong _meta, không chỉ nằm trong văn xuôi', () => {
    const cv = profileToCV(v1, META)
    expect(JSON.parse(cv._meta.droppedFields['/projects/0/tech']!)).toEqual(['Go', 'ONNX'])
  })

  it('phân biệt kỹ năng không gắn nhóm với kỹ năng gắn nhóm tên "Khác"', () => {
    const mixed = ProfileSchema.parse({
      ...v1,
      skills: [{ name: 'Bash' }, { name: 'Vim', group: 'Khác' }],
      _meta: { verified: {}, source: 'manual' },
    })
    const dropped = profileToCV(mixed, META)._meta.droppedFields
    expect(dropped['/skills/0/group']).toBeUndefined()
    expect(dropped['/skills/1/group']).toBe('Khác')
  })
})
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv-migrate.test.ts
```

Kỳ vọng: FAIL — `Cannot find module '../src/cv-migrate.js'`.

- [ ] **Step 3: Viết `cv-migrate.ts` (chiều đi)**

```ts
import type { Profile } from './profile.js'
import { CVSchema, type CV } from './cv.js'

/**
 * Ánh xạ v1 → v2 theo bảng spec 2026-08-09 §2.3.
 *
 * Mọi field v1 không có chỗ ở v2 đều được cất vào `_meta` chứ không vứt: cả
 * mảng `basics.links`, `basics.dob`, `work[].type`, `skills[].level`. Đó là
 * điều kiện để cvToProfile() khôi phục nguyên trạng; vứt đi là đường lùi không
 * còn là đường lùi, và không có đường lùi thì backfill không được phép chạy.
 */

/** Id ổn định theo vị trí: backfill chạy lại phải cho cùng kết quả (idempotent). */
const itemId = (section: string, index: number) => `${section}-${index}`

/**
 * Dịch khoá JSON Pointer của `_meta.verified`.
 *
 * Copy nguyên khoá là hỏng ngầm: `/basics/name` không tồn tại trong v2, nên
 * mọi dấu đã-xác-nhận của người dùng biến mất trong im lặng và UC-22 bắt họ rà
 * soát lại từ đầu.
 *
 * `skills` không nằm trong bảng đổi tên vì nó không phải phép đổi tên: v1 để
 * kỹ năng thành mảng phẳng, v2 gom theo nhóm, nên `/skills/3` phải được TÍNH
 * LẠI thành `/sections/skills/1/skills/0`. `skillPointer` là bảng tra dựng
 * ngay lúc gom nhóm, chỉ số phẳng của v1 → con trỏ v2.
 */
function translateVerifiedPointer(
  pointer: string,
  skillPointer: string[],
): string | null {
  const introField: Record<string, string> = {
    name: 'fullName', headline: 'title', introduce: 'summary',
    email: 'email', phone: 'phone', location: 'location', photo: 'avatarUrl',
  }
  const section: Record<string, string> = {
    work: 'experience', projects: 'projects', education: 'education',
    activities: 'activities', certifications: 'certifications', languages: 'languages',
  }
  const parts = pointer.split('/').filter(Boolean)
  if (parts[0] === 'basics') {
    if (parts.length === 1) return '/sections/intro'
    const mapped = introField[parts[1]!]
    return mapped ? `/sections/intro/${mapped}` : null
  }
  // `/skills/3` và `/skills/3/name` cùng trỏ tới một chuỗi ở v2. Con trỏ tới
  // field khác của kỹ năng không còn đích ở v2 nên trả null.
  if (parts[0] === 'skills') {
    const flat = Number(parts[1])
    if (!Number.isInteger(flat) || !skillPointer[flat]) return null
    if (parts.length === 2 || (parts.length === 3 && parts[2] === 'name')) {
      return skillPointer[flat]!
    }
    return null
  }
  const mappedSection = section[parts[0]!]
  if (!mappedSection) return null
  return ['/sections', mappedSection, ...parts.slice(1)].join('/')
}

export function profileToCV(
  profile: Profile,
  meta: { id: string; title: string; lastModified: string },
): CV {
  const b = profile.basics

  const canonical: Record<string, string> = {}
  for (const s of profile.skills) if (s.canonical) canonical[s.name] = s.canonical

  // Gom theo `group`, giữ nguyên thứ tự nhóm xuất hiện lần đầu — backfill chạy
  // lại phải ra cùng thứ tự, nếu không thì không idempotent.
  // `skillPointer[n]` = con trỏ v2 của kỹ năng thứ n trong mảng phẳng v1. Dựng
  // ngay tại đây vì chỉ chỗ này biết mỗi kỹ năng rơi vào nhóm nào, vị trí nào.
  const grouped: { category: string; skills: string[] }[] = []
  const skillPointer: string[] = []
  profile.skills.forEach((s, flat) => {
    const category = s.group ?? 'Khác'
    let bucket = grouped.findIndex((g) => g.category === category)
    if (bucket === -1) bucket = grouped.push({ category, skills: [] }) - 1
    const pos = grouped[bucket]!.skills.push(s.name) - 1
    skillPointer[flat] = `/sections/skills/${bucket}/skills/${pos}`
  })

  const verified: Record<string, boolean> = {}
  for (const [pointer, value] of Object.entries(profile._meta.verified)) {
    const translated = translateVerifiedPointer(pointer, skillPointer)
    if (translated) verified[translated] = value
  }

  // Khoá là JSON Pointer của v1, nên cvToProfile() đặt lại đúng chỗ mà không
  // phải nhớ một bảng tên riêng.
  const droppedFields: Record<string, string> = {}
  if (b.dob) droppedFields['/basics/dob'] = b.dob
  profile.work.forEach((w, i) => {
    if (w.type) droppedFields[`/work/${i}/type`] = w.type
  })
  profile.skills.forEach((s, i) => {
    if (s.level) droppedFields[`/skills/${i}/level`] = s.level
    // Ghi nhóm thật, không suy từ `category` của v2: "không gắn nhóm" và "gắn
    // nhóm tên đúng là Khác" cho ra cùng một category, suy ngược là nhập hai
    // trạng thái làm một.
    if (s.group) droppedFields[`/skills/${i}/group`] = s.group
  })
  // `tech` lưu thành mảng thật, không mã hoá vào văn xuôi. Dòng "Công nghệ: …"
  // ở highlights[0] chỉ để hiển thị; bóc ngược từ nó thì một bullet do người
  // dùng tự viết mở đầu bằng đúng mấy chữ đó cũng bị nuốt.
  profile.projects.forEach((p, i) => {
    if (p.tech.length) droppedFields[`/projects/${i}/tech`] = JSON.stringify(p.tech)
  })

  return CVSchema.parse({
    schemaVersion: 2,
    id: meta.id,
    title: meta.title,
    lastModified: meta.lastModified,
    language: profile.language,
    sections: {
      intro: {
        fullName: b.name,
        title: b.headline ?? '',
        email: b.email ?? '',
        phone: b.phone ?? '',
        location: b.location ?? '',
        website: b.links[0]?.url,
        summary: b.introduce ?? '',
        avatarUrl: b.photo,
      },
      experience: profile.work.map((w, i) => ({
        id: itemId('exp', i),
        title: w.role,
        company: w.org,
        startDate: w.startDate ?? '',
        endDate: w.endDate ?? '',
        current: !w.endDate,
        highlights: w.highlights,
      })),
      projects: profile.projects.map((p, i) => ({
        id: itemId('proj', i),
        name: p.name,
        role: p.role ?? '',
        startDate: p.startDate ?? '',
        endDate: p.endDate ?? '',
        link: p.url,
        // `tech[]` không có chỗ riêng ở v2; gộp vào bullet đầu để không mất.
        highlights: p.tech.length ? [`Công nghệ: ${p.tech.join(', ')}`, ...p.highlights] : p.highlights,
      })),
      education: profile.education.map((e, i) => ({
        id: itemId('edu', i),
        school: e.school,
        degree: e.degree,
        fieldOfStudy: e.major ?? '',
        startDate: e.startDate ?? '',
        endDate: e.endDate ?? '',
        gpa: e.gpa,
        highlights: e.highlights,
      })),
      skills: grouped.map((g, i) => ({ id: itemId('skill', i), category: g.category, skills: g.skills })),
      activities: profile.activities.map((a, i) => ({
        id: itemId('act', i),
        organization: a.name,
        role: a.role ?? '',
        startDate: a.period ?? '',
        endDate: '',
        highlights: a.highlights,
      })),
      certifications: profile.certifications.map((c, i) => ({
        id: itemId('cert', i),
        name: c.name,
        issuer: c.issuer ?? '',
        date: c.date ?? '',
      })),
      languages: profile.languages.map((l, i) => ({
        id: itemId('lang', i),
        language: l.name,
        proficiency: l.level ?? '',
      })),
    },
    _meta: {
      verified,
      source: profile._meta.source,
      originalLinks: profile.basics.links,
      droppedFields,
      canonical,
    },
  })
}
```

- [ ] **Step 4: Export và chạy test**

Thêm `export * from './cv-migrate.js'` vào `frontend/packages/schema/src/index.ts`.

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv-migrate.test.ts
```

Kỳ vọng: PASS 6/6.

- [ ] **Step 5: Commit**

```bash
git add frontend/packages/schema/src/cv-migrate.ts frontend/packages/schema/src/index.ts \
        frontend/packages/schema/test/cv-migrate.test.ts
git commit -m "feat(schema): chuyển đổi hồ sơ v1 sang CV v2

links[1..] và dob cất vào _meta chứ không vứt: đó là điều kiện để đường
lùi khôi phục nguyên trạng ở task sau.

_meta.verified được DỊCH khoá chứ không copy. Copy nguyên thì mọi dấu
đã-xác-nhận trỏ vào đường dẫn không tồn tại, biến mất trong im lặng, và
UC-22 bắt người dùng rà soát lại từ đầu mà không rõ vì sao.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Đường lùi v2 → v1 và test khứ hồi golden

**Files:**
- Modify: `frontend/packages/schema/src/cv-migrate.ts`
- Modify: `frontend/packages/schema/test/cv-migrate.test.ts`
- Create: `frontend/packages/schema/test/fixtures/profile-v1-full.json`
- Create: `frontend/packages/schema/test/fixtures/profile-v1-minimal.json`
- Create: `frontend/packages/schema/test/fixtures/profile-v1-nogroup.json`
- Create: `frontend/packages/schema/test/fixtures/profile-v1-interleaved.json`

**Interfaces:**
- Consumes: `profileToCV` (Task 2), `ProfileSchema`.
- Produces: `cvToProfile(cv: CV): Profile`. Task 4 gọi từ script rollback.

- [ ] **Step 1: Viết test khứ hồi thất bại**

Bốn dòng `import` dưới đây lên **đầu file** cùng các import đã có; phần `describe` xuống cuối `frontend/packages/schema/test/cv-migrate.test.ts`:

```ts
import { readFileSync, readdirSync } from 'node:fs'
import { join, dirname } from 'node:path'
import { fileURLToPath } from 'node:url'
import { cvToProfile } from '../src/cv-migrate.js'

const FIXTURES = join(dirname(fileURLToPath(import.meta.url)), 'fixtures')

describe('khứ hồi v1 → v2 → v1', () => {
  const files = readdirSync(FIXTURES).filter((f) => f.endsWith('.json'))

  it('có fixture để chạy', () => {
    expect(files.length).toBeGreaterThanOrEqual(4)
  })

  it.each(files)('%s khôi phục nguyên trạng', (file) => {
    const original = ProfileSchema.parse(
      JSON.parse(readFileSync(join(FIXTURES, file), 'utf8')),
    )
    const restored = cvToProfile(profileToCV(original, META))
    expect(restored).toEqual(original)
  })
})
```

- [ ] **Step 2: Tạo ba fixture**

`frontend/packages/schema/test/fixtures/profile-v1-full.json` — hồ sơ đủ mọi mục:

```json
{
  "schemaVersion": 1,
  "language": "vi",
  "basics": {
    "name": "Trần Thị B",
    "headline": "Kỹ sư dữ liệu",
    "introduce": "Bốn năm dựng pipeline dữ liệu",
    "email": "b@example.com",
    "phone": "0912345678",
    "location": "Đà Nẵng",
    "dob": "1998-05-20",
    "photo": "https://cdn.example/b.jpg",
    "links": [
      { "label": "GitHub", "url": "https://github.com/b" },
      { "label": "LinkedIn", "url": "https://linkedin.com/in/b" }
    ]
  },
  "education": [
    { "school": "ĐH Bách khoa", "degree": "Kỹ sư", "major": "CNTT",
      "startDate": "2016-09", "endDate": "2020-06", "gpa": "3.4/4",
      "highlights": ["Đồ án tốt nghiệp loại giỏi"] }
  ],
  "work": [
    { "org": "VNG", "role": "Data Engineer", "type": "fulltime",
      "startDate": "2020-08", "endDate": "2023-04",
      "highlights": ["Dựng pipeline 2TB/ngày", "Giảm 30% chi phí lưu trữ"] },
    { "org": "Momo", "role": "Senior Data Engineer",
      "startDate": "2023-05", "endDate": "",
      "highlights": ["Chuẩn hoá 40 nguồn dữ liệu"] }
  ],
  "projects": [
    { "name": "Realtime CDC", "role": "Tech lead", "tech": ["Debezium", "Kafka"],
      "url": "https://example.dev/cdc", "startDate": "2022-01", "endDate": "2022-09",
      "highlights": ["Độ trễ dưới 2 giây"] }
  ],
  "skills": [
    { "name": "Python", "level": "advanced", "canonical": "python", "group": "Ngôn ngữ" },
    { "name": "SQL", "level": "advanced", "canonical": "sql", "group": "Ngôn ngữ" },
    { "name": "Airflow", "level": "intermediate", "canonical": "apache-airflow", "group": "Điều phối" }
  ],
  "activities": [
    { "name": "GDG Đà Nẵng", "role": "Diễn giả", "period": "2022",
      "highlights": ["Trình bày về CDC cho 120 người"] }
  ],
  "certifications": [
    { "name": "GCP Professional Data Engineer", "issuer": "Google", "date": "2022-11" }
  ],
  "languages": [
    { "name": "Tiếng Anh", "level": "IELTS 7.0" }
  ],
  "_meta": {
    "verified": {
      "/basics/name": true,
      "/basics/email": true,
      "/work/0/highlights/0": true,
      "/education/0": true,
      "/skills/2": true
    },
    "source": "pdf_import"
  }
}
```

`"/skills/2"` trong `verified` là chủ ý: đó là `Airflow`, kỹ năng duy nhất thuộc nhóm "Điều phối", nên chỉ số phẳng của v1 (2) khác hẳn vị trí sau khi gom nhóm (nhóm 1, vị trí 0). Fixture nào cũng gắn dấu vào kỹ năng đầu tiên thì lỗi tính lại chỉ số không bao giờ lộ ra.

`profile-v1-minimal.json` — chỉ có field bắt buộc, mọi mảng rỗng:

```json
{
  "schemaVersion": 1,
  "language": "en",
  "basics": { "name": "Minimal User", "links": [] },
  "education": [], "work": [], "projects": [], "skills": [],
  "activities": [], "certifications": [], "languages": [],
  "_meta": { "verified": {}, "source": "manual" }
}
```

`profile-v1-interleaved.json` — kỹ năng **xen kẽ nhóm**, ca mà mọi fixture xếp sẵn theo nhóm không chạm tới:

```json
{
  "schemaVersion": 1,
  "language": "vi",
  "basics": { "name": "Phạm Thị D", "headline": "Kỹ sư nền tảng", "links": [] },
  "education": [], "work": [], "projects": [],
  "skills": [
    { "name": "Python", "group": "Ngôn ngữ", "level": "advanced" },
    { "name": "Docker", "group": "MLOps", "level": "intermediate" },
    { "name": "Go", "group": "Ngôn ngữ", "canonical": "golang" }
  ],
  "activities": [], "certifications": [], "languages": [],
  "_meta": { "verified": { "/skills/1": true }, "source": "manual" }
}
```

Ba chi tiết đều có chủ đích. Thứ tự `Ngôn ngữ, MLOps, Ngôn ngữ` khiến việc trải phẳng theo nhóm cho ra `[Python, Go, Docker]` — khác gốc, nên khứ hồi đỏ ngay nếu chiều lùi không đọc bảng tra. `level` gắn ở chỉ số 0 và 1 nhưng không có ở 2, nên gán chéo sẽ lộ. `verified` gắn vào `/skills/1` là Docker, kỹ năng duy nhất ở nhóm thứ hai.

`profile-v1-nogroup.json` — kỹ năng **không có** `group`, để chứng minh nhóm mặc định "Khác" cũng khứ hồi được:

```json
{
  "schemaVersion": 1,
  "language": "vi",
  "basics": { "name": "Lê Văn C", "headline": "Fresher", "links": [] },
  "education": [], "work": [], "projects": [],
  "skills": [
    { "name": "HTML", "canonical": "html" },
    { "name": "CSS" }
  ],
  "activities": [], "certifications": [], "languages": [],
  "_meta": { "verified": {}, "source": "manual" }
}
```

- [ ] **Step 3: Chạy để thấy nó fail**

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv-migrate.test.ts
```

Kỳ vọng: FAIL — `cvToProfile` chưa tồn tại.

- [ ] **Step 4: Viết `cvToProfile`**

Thêm vào cuối `frontend/packages/schema/src/cv-migrate.ts`:

```ts
import { ProfileSchema } from './profile.js'

/**
 * Nghịch đảo của translateVerifiedPointer().
 *
 * Chỉ số phẳng của v1 KHÔNG tính lại được từ v2. Chiều đi gom nhóm, nên với
 * `[Python(A), Docker(B), Go(A)]` thì v2 là `[[Python,Go],[Docker]]`; trải
 * phẳng lại theo thứ tự nhóm ra `[Python, Go, Docker]` — sai thứ tự gốc, và
 * mọi khoá `/skills/N/level`, `/skills/N/group` gán chéo sang kỹ năng khác.
 * Thứ tự xen kẽ không còn tồn tại trong tài liệu v2, nên không công thức nào
 * dựng lại được. Chiều đi phải ghi bảng tra vào `_meta.droppedFields`
 * `['/skills/_order']`, và đây là chỗ đọc nó.
 *
 * `order[n]` = con trỏ v2 của kỹ năng ở chỉ số phẳng n của v1.
 */
function untranslateVerifiedPointer(
  pointer: string,
  order: string[],
): string | null {
  if (/^\/sections\/skills\/\d+\/skills\/\d+$/.test(pointer)) {
    const flat = order.indexOf(pointer)
    return flat === -1 ? null : `/skills/${flat}`
  }
  const introField: Record<string, string> = {
    fullName: 'name', title: 'headline', summary: 'introduce',
    email: 'email', phone: 'phone', location: 'location', avatarUrl: 'photo',
  }
  const section: Record<string, string> = {
    experience: 'work', projects: 'projects', education: 'education',
    activities: 'activities', certifications: 'certifications', languages: 'languages',
  }
  const parts = pointer.split('/').filter(Boolean)
  if (parts[0] !== 'sections') return null
  if (parts[1] === 'intro') {
    if (parts.length === 2) return '/basics'
    const mapped = introField[parts[2]!]
    return mapped ? `/basics/${mapped}` : null
  }
  const mapped = section[parts[1]!]
  return mapped ? ['', mapped, ...parts.slice(2)].join('/') : null
}

/**
 * v2 → v1. Đọc `_meta.originalLinks`, `_meta.droppedFields` và `_meta.canonical`
 * để dựng lại đúng những gì chiều đi đã cất đi.
 *
 * Test khứ hồi trong cv-migrate.test.ts là chốt chặn: v1 → v2 → v1 phải khớp
 * tuyệt đối. Chốt đó hỏng nghĩa là backfill không có đường lùi, và không có
 * đường lùi thì không được phép chạy trên dữ liệu thật.
 */
export function cvToProfile(cv: CV): Profile {
  const intro = cv.sections.intro
  const dropped = cv._meta.droppedFields

  // Lấy thẳng từ _meta chứ không dựng lại từ `website`: nhãn của link đầu tiên
  // không tồn tại ở v2, dựng lại là đoán.
  const links = cv._meta.originalLinks

  // Dựng lại mảng kỹ năng THEO BẢNG TRA, không trải phẳng theo thứ tự nhóm.
  // Trải phẳng là sai thứ tự ngay khi hồ sơ gốc xen kẽ nhóm, và mọi khoá
  // /skills/N/... gán chéo sang kỹ năng khác — xem ghi chú ở
  // untranslateVerifiedPointer().
  //
  // `category` của v2 chỉ là nhãn hiển thị. Nhóm thật đọc từ droppedFields: kỹ
  // năng không gắn nhóm và kỹ năng gắn nhóm tên đúng là "Khác" ra cùng một
  // category, nên suy ngược từ category là nhập hai trạng thái làm một.
  const order: string[] = dropped['/skills/_order']
    ? (JSON.parse(dropped['/skills/_order']) as string[])
    : []

  const skillNameAt = (pointer: string): string | null => {
    const m = pointer.match(/^\/sections\/skills\/(\d+)\/skills\/(\d+)$/)
    if (!m) return null
    return cv.sections.skills[Number(m[1])]?.skills[Number(m[2])] ?? null
  }

  const skills = order.flatMap((pointer, i) => {
    const name = skillNameAt(pointer)
    if (name === null) return []
    const level = dropped[`/skills/${i}/level`]
    const originalGroup = dropped[`/skills/${i}/group`]
    return [{
      name,
      ...(level ? { level } : {}),
      ...(cv._meta.canonical[name] ? { canonical: cv._meta.canonical[name] } : {}),
      ...(originalGroup ? { group: originalGroup } : {}),
    }]
  })

  const verified: Record<string, boolean> = {}
  for (const [pointer, value] of Object.entries(cv._meta.verified)) {
    const back = untranslateVerifiedPointer(pointer, order)
    if (back) verified[back] = value
  }

  return ProfileSchema.parse({
    schemaVersion: 1,
    language: cv.language,
    basics: {
      name: intro.fullName,
      ...(intro.title ? { headline: intro.title } : {}),
      ...(intro.summary ? { introduce: intro.summary } : {}),
      ...(intro.email ? { email: intro.email } : {}),
      ...(intro.phone ? { phone: intro.phone } : {}),
      ...(intro.location ? { location: intro.location } : {}),
      ...(dropped['/basics/dob'] ? { dob: dropped['/basics/dob'] } : {}),
      ...(intro.avatarUrl ? { photo: intro.avatarUrl } : {}),
      links,
    },
    education: cv.sections.education.map((e) => ({
      school: e.school,
      degree: e.degree,
      ...(e.fieldOfStudy ? { major: e.fieldOfStudy } : {}),
      ...(e.startDate ? { startDate: e.startDate } : {}),
      ...(e.endDate ? { endDate: e.endDate } : {}),
      ...(e.gpa ? { gpa: e.gpa } : {}),
      highlights: e.highlights,
    })),
    work: cv.sections.experience.map((x, i) => ({
      org: x.company,
      role: x.title,
      ...(dropped[`/work/${i}/type`] ? { type: dropped[`/work/${i}/type`] } : {}),
      ...(x.startDate ? { startDate: x.startDate } : {}),
      ...(x.endDate ? { endDate: x.endDate } : {}),
      highlights: x.highlights,
    })),
    // `tech` đọc từ droppedFields chứ không bóc từ chuỗi "Công nghệ: …".
    // Bóc chuỗi thì một bullet do người dùng tự viết mở đầu bằng đúng mấy chữ
    // đó cũng bị nuốt, và tech nào chứa ", " sẽ bị tách sai. Dòng hiển thị ở
    // highlights[0] chỉ là bản trình bày, dữ liệu thật nằm trong _meta.
    projects: cv.sections.projects.map((p, i) => {
      const rawTech = dropped[`/projects/${i}/tech`]
      const tech = rawTech ? (JSON.parse(rawTech) as string[]) : []
      return {
        name: p.name,
        ...(p.role ? { role: p.role } : {}),
        tech,
        ...(p.link ? { url: p.link } : {}),
        ...(p.startDate ? { startDate: p.startDate } : {}),
        ...(p.endDate ? { endDate: p.endDate } : {}),
        highlights: rawTech ? p.highlights.slice(1) : p.highlights,
      }
    }),
    skills,
    activities: cv.sections.activities.map((a) => ({
      name: a.organization,
      ...(a.role ? { role: a.role } : {}),
      ...(a.startDate ? { period: a.startDate } : {}),
      highlights: a.highlights,
    })),
    certifications: cv.sections.certifications.map((c) => ({
      name: c.name,
      ...(c.issuer ? { issuer: c.issuer } : {}),
      ...(c.date ? { date: c.date } : {}),
    })),
    languages: cv.sections.languages.map((l) => ({
      name: l.language,
      ...(l.proficiency ? { level: l.proficiency } : {}),
    })),
    _meta: { verified, source: cv._meta.source },
  })
}
```

- [ ] **Step 5: Chạy test khứ hồi**

```bash
cd frontend && npx vitest run --project unit packages/schema/test/cv-migrate.test.ts
```

Kỳ vọng: PASS toàn bộ, gồm 3 ca khứ hồi.

**Nếu một fixture lệch:** đừng sửa test cho khớp code, và đừng sửa fixture cho khớp code. Lệch nghĩa là chiều đi đang vứt dữ liệu — sửa `profileToCV` để cất thứ bị mất vào `_meta.droppedFields` với khoá là JSON Pointer v1, rồi sửa `cvToProfile` đặt nó về đúng chỗ. Đây chính là việc mà `/work/N/type` và `/skills/N/level` đã làm ở Task 2; ca lệch mới cũng theo đúng khuôn đó.

- [ ] **Step 6: Commit**

```bash
git add frontend/packages/schema/src/cv-migrate.ts \
        frontend/packages/schema/test/cv-migrate.test.ts \
        frontend/packages/schema/test/fixtures/
git commit -m "feat(schema): đường lùi v2 về v1, chứng minh bằng khứ hồi golden

Ba fixture: hồ sơ đủ mục, hồ sơ tối thiểu, và hồ sơ có kỹ năng không gắn
nhóm. v1 → v2 → v1 phải khớp tuyệt đối.

Viết đường lùi cùng lúc với chiều đi chứ không để sau: backfill không có
đường lùi thì không được phép chạy trên dữ liệu thật, nên hoãn nó lại là
hoãn luôn cả việc dùng được chiều đi.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Migration 010 và script backfill hai chiều

**Files:**
- Create: `backend/db/migrations/010_cv_schema_v2.sql`
- Create: `backend/db/backfill-v2.ts`
- Modify: `frontend/package.json` (thêm script `db:backfill-v2`)

**Interfaces:**
- Consumes: `profileToCV`, `cvToProfile` (Task 2, 3); `pg` (đã có).
- Produces: cột `profiles.data_v2 jsonb`, `cv_documents.snapshot_v2 jsonb`; lệnh `npm run db:backfill-v2 -- [--rollback] [--dry-run]`.

- [ ] **Step 1: Viết migration**

Tạo `backend/db/migrations/010_cv_schema_v2.sql`:

```sql
-- CV schema v2 — spec 2026-08-09 §2.4.
--
-- CHỈ THÊM, không sửa và không xoá gì. `apps/web` đọc `profiles.data` theo v1
-- và phải phục vụ production tới SP-5; đổi tại chỗ là làm chết nó ngay.
--
-- SP-5 lật công tắc: đổi tên `data_v2` thành `data` và bỏ cột cũ. Tới lúc đó
-- mới xoá, không sớm hơn.

ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS data_v2 jsonb;

ALTER TABLE cv_documents
  ADD COLUMN IF NOT EXISTS snapshot_v2 jsonb;

-- Chỉ đánh index phần đã backfill: giai đoạn chuyển tiếp phần lớn hàng còn NULL.
CREATE INDEX IF NOT EXISTS profiles_v2_ready_idx
  ON profiles (id) WHERE data_v2 IS NOT NULL;

-- Hàng nào có data_v2 thì bắt buộc đúng phiên bản. Chặn ngay tại tầng lưu trữ,
-- không đợi tầng ứng dụng phát hiện.
ALTER TABLE profiles
  ADD CONSTRAINT profiles_data_v2_version_chk
  CHECK (data_v2 IS NULL OR (data_v2->>'schemaVersion') = '2');
```

- [ ] **Step 2: Chạy migration**

```bash
cd frontend && npm run db:migrate
```

Kỳ vọng: `→ 010_cv_schema_v2 ... ✓`.

Kiểm chứng cột đã có và dữ liệu cũ không đổi:

```bash
docker exec hr-postgres psql -U postgres -d hragent -c "\d profiles" | grep data_v2
docker exec hr-postgres psql -U postgres -d hragent -tAc \
  "SELECT count(*) FROM profiles WHERE data IS NOT NULL;"
```

Kỳ vọng: thấy dòng `data_v2 | jsonb`, và số hàng có `data` vẫn là **24** như trước khi chạy.

- [ ] **Step 3: Viết script backfill**

Tạo `backend/db/backfill-v2.ts`:

```ts
#!/usr/bin/env tsx
/**
 * Backfill v1 → v2 (và ngược lại).
 *
 * Chạy:
 *   npm run db:backfill-v2                # v1 → v2
 *   npm run db:backfill-v2 -- --dry-run   # chỉ đếm, không ghi
 *   npm run db:backfill-v2 -- --rollback  # v2 → v1, dựng lại `data` từ `data_v2`
 *
 * IDEMPOTENT: chạy n lần cho cùng kết quả như chạy một lần. Không có tính chất
 * này thì không ai dám chạy lại sau khi nó hỏng giữa chừng, mà hỏng giữa chừng
 * là chuyện sẽ xảy ra.
 */
import { Client } from 'pg'
import { ProfileSchema, CVSchema, profileToCV, cvToProfile } from '@hr/schema'

const args = new Set(process.argv.slice(2))
const dryRun = args.has('--dry-run')
const rollback = args.has('--rollback')

const url =
  process.env.DATABASE_URL ?? 'postgres://postgres:hragent_dev@localhost:5433/hragent'
const client = new Client({ connectionString: url })
await client.connect()

let ok = 0
let skipped = 0
const failures: { id: string; reason: string }[] = []

if (!rollback) {
  const { rows } = await client.query<{ id: string; data: unknown; updated_at: Date }>(
    'SELECT id, data, updated_at FROM profiles ORDER BY id',
  )
  for (const row of rows) {
    try {
      const profile = ProfileSchema.parse(row.data)
      const cv = profileToCV(profile, {
        id: row.id,
        title: profile.basics.name ? `CV của ${profile.basics.name}` : 'CV của tôi',
        // Lấy từ cột updated_at chứ không phải giờ hiện tại: dùng giờ hiện tại
        // thì mỗi lần chạy lại ra một giá trị khác và script hết idempotent.
        lastModified: row.updated_at.toISOString(),
      })
      if (dryRun) { ok++; continue }
      await client.query('UPDATE profiles SET data_v2 = $2::jsonb WHERE id = $1', [
        row.id,
        JSON.stringify(cv),
      ])
      ok++
    } catch (err) {
      failures.push({ id: row.id, reason: (err as Error).message })
    }
  }
} else {
  const { rows } = await client.query<{ id: string; data_v2: unknown }>(
    'SELECT id, data_v2 FROM profiles WHERE data_v2 IS NOT NULL ORDER BY id',
  )
  for (const row of rows) {
    try {
      const profile = cvToProfile(CVSchema.parse(row.data_v2))
      if (dryRun) { ok++; continue }
      await client.query('UPDATE profiles SET data = $2::jsonb WHERE id = $1', [
        row.id,
        JSON.stringify(profile),
      ])
      ok++
    } catch (err) {
      failures.push({ id: row.id, reason: (err as Error).message })
    }
  }
}

console.log(`${rollback ? 'Rollback' : 'Backfill'}${dryRun ? ' (dry-run)' : ''}: ` +
  `${ok} thành công, ${skipped} bỏ qua, ${failures.length} lỗi.`)
for (const f of failures) console.error(`  ✗ ${f.id}: ${f.reason}`)

await client.end()
// Có một hàng hỏng cũng là thất bại: im lặng bỏ qua nghĩa là hàng đó ở lại v1
// mãi mãi, và không ai biết cho tới lúc SP-5 lật công tắc.
process.exit(failures.length ? 1 : 0)
```

- [ ] **Step 4: Thêm script vào `frontend/package.json`**

Ngay dưới dòng `"db:migrate"`, thêm:

```json
    "db:backfill-v2": "tsx ../backend/db/backfill-v2.ts",
```

- [ ] **Step 5: Chạy dry-run trước**

```bash
cd frontend && npm run db:backfill-v2 -- --dry-run
```

Kỳ vọng: `Backfill (dry-run): 24 thành công, 0 bỏ qua, 0 lỗi.`

Có hàng lỗi thì **dừng lại** và đọc lý do — hồ sơ thật có hình dạng mà fixture chưa phủ. Thêm fixture cho ca đó ở Task 3, sửa converter, chạy lại.

- [ ] **Step 6: Chạy thật, rồi chứng minh idempotent**

```bash
cd frontend && npm run db:backfill-v2
docker exec hr-postgres psql -U postgres -d hragent -tAc \
  "SELECT md5(string_agg(data_v2::text, '' ORDER BY id)) FROM profiles WHERE data_v2 IS NOT NULL;"
npm run db:backfill-v2
docker exec hr-postgres psql -U postgres -d hragent -tAc \
  "SELECT md5(string_agg(data_v2::text, '' ORDER BY id)) FROM profiles WHERE data_v2 IS NOT NULL;"
```

Kỳ vọng: hai chuỗi md5 **giống hệt nhau**. Khác nhau là script chưa idempotent — thủ phạm hầu như luôn là một giá trị phụ thuộc thời gian chạy.

- [ ] **Step 6b: Chứng minh đường lùi bằng kiểm tra CHỈ ĐỌC, không chạy rollback lên dữ liệu thật**

> **Đã xảy ra thật ngày 2026-08-09.** Bản đầu của plan này viết "chạy `--rollback` thật".
> Nó chạy, ghi đè `profiles.data` của cả 24 hàng, và làm mất `_meta.verified['/skills']`
> ở 3 hồ sơ cùng `basics.summary` ở 2 hồ sơ. Khôi phục được là nhờ
> `cv_documents.profile_snapshot` tình cờ không bị đụng — một may mắn, không phải
> thiết kế. Diễn tập đường lùi **không bao giờ** chạy lên dữ liệu thật.

Viết một script chỉ đọc: với mỗi hàng, so `raw → v2 → v1` với chính `raw` **thô lấy từ
DB**, không phải với bản đã qua `ProfileSchema.parse`. So với bản đã parse là tự bịt mắt:
Zod lược khoá lạ trước khi so, nên mọi khoá bị mất đều biến khỏi cả hai vế.

```bash
cd frontend && npx tsx ../backend/db/roundtrip-check.ts
```

Kỳ vọng: `24/24 khớp tuyệt đối`. Lệch một hàng là đường lùi chưa dùng được — sửa cho hết
lệch rồi mới đi tiếp, đừng chạy `--rollback` để "xem thử".

- [ ] **Step 7: Chứng minh `data` v1 không bị đụng**

```bash
docker exec hr-postgres psql -U postgres -d hragent -tAc \
  "SELECT count(*) FROM profiles WHERE data->>'schemaVersion' = '1';"
curl -sf http://localhost:3000 >/dev/null && echo "hr-web còn sống"
```

Kỳ vọng: vẫn **24**, và `hr-web` trả về được.

- [ ] **Step 8: Commit**

```bash
git add backend/db/migrations/010_cv_schema_v2.sql backend/db/backfill-v2.ts frontend/package.json
git commit -m "feat(db): migration 010 thêm cột v2 và script backfill hai chiều

Thêm data_v2 cạnh data thay vì đổi tại chỗ. apps/web đọc data theo v1 và
phải phục vụ production tới SP-5; đổi tại chỗ là làm chết nó ngay. SP-5
lật công tắc rồi mới xoá cột cũ.

lastModified lấy từ cột updated_at chứ không phải giờ chạy script: dùng
giờ chạy thì mỗi lượt ra một giá trị khác và script hết idempotent, mà
hết idempotent thì không ai dám chạy lại sau khi nó hỏng giữa chừng.

Một hàng lỗi cũng thoát mã khác 0: bỏ qua trong im lặng nghĩa là hàng đó
kẹt ở v1 mãi, và không ai biết cho tới lúc cutover.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Go che được PII của cả hai hình dạng

**Files:**
- Modify: `backend/internal/pii/pii.go`
- Create: `backend/internal/pii/pii_test.go`
- Modify: `backend/internal/api/server.go` (hàm `redactProfileForModel`)

**Interfaces:**
- Consumes: —
- Produces: `pii.ProfileKeys`, `pii.IntroKeys`, `pii.RedactBasics`, `pii.RedactDocument(obj map[string]any)`. Task 7 dùng `RedactDocument`.

- [ ] **Step 1: Viết test thất bại**

Tạo `backend/internal/pii/pii_test.go`:

```go
package pii

import "testing"

func TestRedactDocumentHandlesV1Shape(t *testing.T) {
	doc := map[string]any{
		"basics": map[string]any{"name": "Ada", "email": "a@x.com", "headline": "CTO"},
		"work":   []any{map[string]any{"org": "FPT"}},
	}

	RedactDocument(doc)

	basics := doc["basics"].(map[string]any)
	if _, leaked := basics["name"]; leaked {
		t.Fatalf("basics.name còn lại: %#v", doc)
	}
	if basics["headline"] != "CTO" {
		t.Fatalf("field phi-PII bị xoá nhầm: %#v", doc)
	}
}

func TestRedactDocumentHandlesV2Shape(t *testing.T) {
	doc := map[string]any{
		"schemaVersion": float64(2),
		"sections": map[string]any{
			"intro": map[string]any{
				"fullName": "Ada", "email": "a@x.com", "phone": "090",
				"location": "Hà Nội", "avatarUrl": "https://cdn/x.jpg",
				"title": "CTO", "summary": "Mười năm", "website": "https://ada.dev",
			},
		},
	}

	RedactDocument(doc)

	intro := doc["sections"].(map[string]any)["intro"].(map[string]any)
	for _, key := range IntroKeys {
		if _, leaked := intro[key]; leaked {
			t.Fatalf("sections.intro.%s còn lại: %#v", key, doc)
		}
	}
	// website và summary KHÔNG phải PII — model cần chúng để đề xuất có nghĩa.
	if intro["title"] != "CTO" || intro["summary"] != "Mười năm" || intro["website"] != "https://ada.dev" {
		t.Fatalf("nội dung nghề nghiệp bị xoá nhầm: %#v", doc)
	}
}

// Danh sách v2 phải khớp PII_PATHS_V2 ở frontend/packages/schema/src/cv.ts.
// Lệch một dòng là PII đi thẳng ra cloud mà không lỗi nào được ném ra.
func TestIntroKeysMatchSpecList(t *testing.T) {
	want := []string{"fullName", "email", "phone", "location", "avatarUrl"}
	if len(IntroKeys) != len(want) {
		t.Fatalf("IntroKeys = %v, want %v", IntroKeys, want)
	}
	for i, key := range want {
		if IntroKeys[i] != key {
			t.Fatalf("IntroKeys[%d] = %q, want %q", i, IntroKeys[i], key)
		}
	}
}
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd backend && go test ./internal/pii/
```

Kỳ vọng: FAIL — `undefined: RedactDocument`, `undefined: IntroKeys`.

- [ ] **Step 3: Bổ sung `pii.go`**

Thêm vào `backend/internal/pii/pii.go`:

```go
// IntroKeys là các khoá PII trong `sections.intro` của CV v2.
// Phải khớp PII_PATHS_V2 ở frontend/packages/schema/src/cv.ts.
//
// `website` và `summary` KHÔNG có ở đây: chúng là nội dung nghề nghiệp, model
// cần đọc để đề xuất có nghĩa. Che quá tay cũng là hỏng, chỉ theo hướng khác.
var IntroKeys = []string{"fullName", "email", "phone", "location", "avatarUrl"}

// RedactIntro xoá mọi khoá PII khỏi `sections.intro` của v2, sửa tại chỗ.
func RedactIntro(intro map[string]any) {
	for _, key := range IntroKeys {
		delete(intro, key)
	}
}

// RedactDocument nhận diện hình dạng rồi che đúng chỗ.
//
// Nhận diện bằng dữ liệu chứ không bằng cờ phiên bản: trong giai đoạn chuyển
// tiếp, hồ sơ v1 và v2 cùng đi qua một binary, và một hồ sơ thiếu
// `schemaVersion` không được phép trở thành hồ sơ không bị che.
func RedactDocument(doc map[string]any) {
	if sections, ok := doc["sections"].(map[string]any); ok {
		if intro, ok := sections["intro"].(map[string]any); ok {
			RedactIntro(intro)
		}
	}
	if basics, ok := doc["basics"].(map[string]any); ok {
		RedactBasics(basics)
	}
}
```

- [ ] **Step 4: Cho `server.go` dùng `RedactDocument`**

Trong `backend/internal/api/server.go`, hàm `redactProfileForModel`, đổi thân hàm:

```go
	if basics, ok := obj["basics"].(map[string]any); ok {
		pii.RedactBasics(basics)
	}
```

thành:

```go
	pii.RedactDocument(obj)
```

- [ ] **Step 5: Chạy test**

```bash
cd backend && go test ./...
```

Kỳ vọng: xanh toàn bộ, gồm cả `TestChatPromptNeverCarriesPIIToModel` đã có.

- [ ] **Step 6: Commit**

```bash
git add backend/internal/pii/ backend/internal/api/server.go
git commit -m "feat(pii): che được PII của cả hồ sơ v1 lẫn CV v2

Nhận diện hình dạng bằng dữ liệu chứ không bằng cờ schemaVersion: giai
đoạn chuyển tiếp có cả hai hình dạng đi qua cùng một binary, và một hồ sơ
thiếu cờ phiên bản không được phép trở thành hồ sơ không bị che.

website và summary không nằm trong danh sách PII — chúng là nội dung nghề
nghiệp, model cần đọc để đề xuất có nghĩa. Che quá tay cũng là hỏng.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Worker đối chiếu JD đọc được cả hai hình dạng

**Files:**
- Modify: `backend/cmd/worker/matching.go:195-225` (`profileChunks`), `:505`, `:588`
- Modify: `backend/cmd/worker/main_test.go`

**Interfaces:**
- Consumes: `pii` (Task 5).
- Produces: `profileChunks` trả cùng kiểu như trước, thêm nhánh v2. Không đổi chữ ký.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `backend/cmd/worker/main_test.go`:

```go
// Con trỏ trong kết quả đối chiếu được hiển thị cho người dùng và dùng để nhảy
// tới đúng dòng trong CV. Trỏ sai đường dẫn thì tính năng "nhảy tới chỗ thiếu"
// đưa người dùng đi đâu đó không xác định.
func TestProfileChunksReadsV2Sections(t *testing.T) {
	v2 := map[string]any{
		"schemaVersion": float64(2),
		"sections": map[string]any{
			"intro": map[string]any{"title": "Kỹ sư AI", "summary": "Ba năm edge AI"},
			"experience": []any{map[string]any{
				"title": "Engineer", "company": "FPT",
				"highlights": []any{"Giảm 40% độ trễ"},
			}},
			"skills": []any{map[string]any{
				"category": "Ngôn ngữ", "skills": []any{"Go", "Python"},
			}},
		},
	}

	chunks := profileChunks(v2)

	want := map[string]string{
		"/sections/intro/title":                   "Kỹ sư AI",
		"/sections/intro/summary":                 "Ba năm edge AI",
		"/sections/experience/0/highlights/0":     "Giảm 40% độ trễ",
		"/sections/skills/0/skills/0":             "Go",
	}
	got := map[string]string{}
	for _, c := range chunks {
		got[c.Path] = c.Text
	}
	for path, text := range want {
		if got[path] != text {
			t.Fatalf("chunk %q = %q, want %q\ntoàn bộ: %#v", path, got[path], text, got)
		}
	}
}

// Hồ sơ v1 vẫn phải chạy: apps/web dùng nó tới SP-5.
func TestProfileChunksStillReadsV1(t *testing.T) {
	v1 := map[string]any{
		"basics": map[string]any{"headline": "Kỹ sư AI", "introduce": "Ba năm edge AI"},
		"work": []any{map[string]any{
			"role": "Engineer", "org": "FPT",
			"highlights": []any{"Giảm 40% độ trễ"},
		}},
	}

	got := map[string]string{}
	for _, c := range profileChunks(v1) {
		got[c.Path] = c.Text
	}
	if got["/basics/headline"] != "Kỹ sư AI" || got["/work/0/highlights/0"] != "Giảm 40% độ trễ" {
		t.Fatalf("nhánh v1 hỏng: %#v", got)
	}
}
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd backend && go test ./cmd/worker/ -run "ProfileChunks"
```

Kỳ vọng: `TestProfileChunksReadsV2Sections` FAIL (không có chunk nào), `TestProfileChunksStillReadsV1` PASS.

Nếu ca v1 cũng fail: chữ ký `profileChunks` khác giả định của test — đọc `matching.go:195` và sửa test cho khớp kiểu thật **trước khi** viết code.

- [ ] **Step 3: Thêm nhánh v2 vào `profileChunks`**

Ở đầu thân hàm `profileChunks` trong `backend/cmd/worker/matching.go`, thêm:

```go
	// Giai đoạn chuyển tiếp SP-2→SP-5: hai hình dạng cùng đi qua worker này.
	// Nhận diện bằng sự có mặt của `sections`, không bằng cờ schemaVersion —
	// hồ sơ thiếu cờ không được phép rơi vào nhánh sai và trả về rỗng lặng lẽ.
	if sections, ok := p["sections"].(map[string]any); ok {
		return profileChunksV2(sections)
	}
```

Rồi thêm hàm mới ngay dưới `profileChunks`:

```go
func profileChunksV2(sections map[string]any) []chunk {
	var out []chunk
	if intro, ok := sections["intro"].(map[string]any); ok {
		appendChunk(&out, "/sections/intro/title", stringValue(intro["title"]))
		appendChunk(&out, "/sections/intro/summary", stringValue(intro["summary"]))
	}
	for _, section := range []struct{ key, name string }{
		{"experience", "title"}, {"projects", "name"}, {"activities", "organization"},
	} {
		items, _ := sections[section.key].([]any)
		for i, item := range items {
			m, ok := item.(map[string]any)
			if !ok {
				continue
			}
			base := fmt.Sprintf("/sections/%s/%d", section.key, i)
			appendChunk(&out, base+"/"+section.name, stringValue(m[section.name]))
			for j, h := range stringArray(m["highlights"]) {
				appendChunk(&out, fmt.Sprintf("%s/highlights/%d", base, j), h)
			}
		}
	}
	// skills v2 là nhóm: một dòng chứa nhiều kỹ năng, mỗi kỹ năng một chunk để
	// lớp keyword đối chiếu được từng cái một.
	groups, _ := sections["skills"].([]any)
	for i, group := range groups {
		m, ok := group.(map[string]any)
		if !ok {
			continue
		}
		for j, name := range stringArray(m["skills"]) {
			appendChunk(&out, fmt.Sprintf("/sections/skills/%d/skills/%d", i, j), name)
		}
	}
	return out
}
```

- [ ] **Step 4: Chạy test**

```bash
cd backend && go test ./cmd/worker/ -v -run "ProfileChunks"
```

Kỳ vọng: PASS cả hai.

- [ ] **Step 5: Chạy toàn bộ test Go**

```bash
cd backend && go test ./... && gofmt -l ./cmd ./internal && go vet ./...
```

Kỳ vọng: xanh, `gofmt -l` không in tên file nào.

- [ ] **Step 6: Commit**

```bash
git add backend/cmd/worker/matching.go backend/cmd/worker/main_test.go
git commit -m "feat(worker): đối chiếu JD đọc được cả hồ sơ v1 lẫn CV v2

Nhận diện bằng sự có mặt của khoá sections, không bằng cờ schemaVersion:
hồ sơ thiếu cờ không được phép rơi vào nhánh sai rồi trả về rỗng trong im
lặng — điểm khớp 0 trông y hệt CV không khớp gì.

skills v2 là nhóm nên mỗi kỹ năng vẫn thành một chunk riêng, giữ nguyên
độ hạt của lớp đối chiếu keyword (BR-57.1).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Prompt và validate chọn theo phiên bản hồ sơ

**Files:**
- Modify: `backend/internal/api/server.go` (`chatSystemPrompt`, `validateChatProposal`, chỗ gọi ở handler chat)
- Modify: `backend/internal/api/server_test.go`

**Interfaces:**
- Consumes: `pii.RedactDocument` (Task 5).
- Produces: `chatSystemPrompt(v2 bool) string`, `profileIsV2(raw []byte) bool`.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `backend/internal/api/server_test.go`:

```go
func TestProfileIsV2DetectsShape(t *testing.T) {
	if profileIsV2([]byte(`{"basics":{"name":"Ada"}}`)) {
		t.Fatal("hồ sơ v1 bị nhận nhầm là v2")
	}
	if !profileIsV2([]byte(`{"sections":{"intro":{"fullName":"Ada"}}}`)) {
		t.Fatal("CV v2 không được nhận ra")
	}
	// Rác không được rơi vào nhánh v2: nhánh sai thì prompt dạy model sinh con
	// trỏ không tồn tại, và mọi đề xuất bị chốt chặn loại sạch.
	if profileIsV2([]byte(`không phải JSON`)) {
		t.Fatal("dữ liệu hỏng phải rơi về v1")
	}
}

func TestChatSystemPromptV2UsesSectionPointers(t *testing.T) {
	v2 := chatSystemPrompt(true)
	if !strings.Contains(v2, "/sections/intro/summary") {
		t.Fatal("prompt v2 phải dạy đường dẫn giới thiệu của v2")
	}
	if strings.Contains(v2, "/basics/introduce") {
		t.Fatal("prompt v2 không được nhắc đường dẫn v1")
	}
	if !strings.Contains(v2, "/sections/experience/0/highlights/") {
		t.Fatal("prompt v2 phải chỉ rõ cách nhắm vào một gạch đầu dòng")
	}

	v1 := chatSystemPrompt(false)
	if !strings.Contains(v1, "/basics/introduce") {
		t.Fatal("prompt v1 phải giữ nguyên: apps/web dùng nó tới SP-5")
	}
}
```

Sửa test đã có `TestChatPromptUsesIntroduceForCVField` cho khớp chữ ký mới:

```go
func TestChatPromptUsesIntroduceForCVField(t *testing.T) {
	prompt := chatSystemPrompt(false)
	if !strings.Contains(prompt, "/basics/introduce") {
		t.Fatal("chat prompt must identify the CV introduction field")
	}
	if strings.Contains(prompt, "/basics/summary") {
		t.Fatal("chat prompt must not instruct the model to write the legacy CV summary field")
	}
}
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd backend && go test ./internal/api/
```

Kỳ vọng: FAIL biên dịch — `undefined: profileIsV2` và `chatSystemPrompt` nhận 0 tham số.

- [ ] **Step 3: Sửa `server.go`**

Thêm hàm nhận diện, đặt ngay trên `chatSystemPrompt`:

```go
// Nhận diện hình dạng hồ sơ bằng dữ liệu. Dữ liệu hỏng rơi về v1: nhánh sai thì
// prompt dạy model sinh con trỏ không tồn tại, chốt chặn loại sạch mọi đề xuất,
// và người dùng chỉ thấy trợ lý "không làm được gì" mà không rõ vì sao.
func profileIsV2(raw []byte) bool {
	var probe struct {
		Sections map[string]any `json:"sections"`
	}
	if json.Unmarshal(raw, &probe) != nil {
		return false
	}
	return probe.Sections != nil
}
```

Đổi `chatSystemPrompt()` thành `chatSystemPrompt(v2 bool)`, giữ nguyên phần thân cũ cho nhánh `false`, và thêm nhánh `true`:

```go
func chatSystemPrompt(v2 bool) string {
	if v2 {
		return `Bạn là trợ lý chỉnh CV. Trả lời cùng ngôn ngữ với hồ sơ và chỉ trả về DUY NHẤT một object JSON hợp lệ.

Nếu người dùng chỉ hỏi hoặc muốn xem giải thích, trả:
{"kind":"reply","text":"..."}

Nếu người dùng yêu cầu sửa, viết lại, nhóm, sắp xếp hoặc cập nhật hồ sơ, KHÔNG được nói đã cập nhật. Hãy trả đề xuất để người dùng duyệt:
{"kind":"patch","summary":"...","ops":[{"op":"add|replace|remove","path":"/sections/experience/0/highlights/2","value":"...","rationale":"...","grounding":{"type":"existing_field|user_message|kb|inference","ref":"..."},"kbRefs":[]}]}

Quy tắc bắt buộc: không tự ghi hồ sơ; không bịa dữ kiện; tối đa 20 ops; value bắt buộc với add/replace; mỗi op/path chỉ xuất hiện một lần.

Đường dẫn: phần giới thiệu cá nhân luôn là /sections/intro/summary. Mỗi gạch đầu dòng của kinh nghiệm, dự án, học vấn và hoạt động là một phần tử riêng — sửa một ý thì nhắm đúng vào nó, ví dụ /sections/experience/0/highlights/2, KHÔNG ghi đè cả mảng highlights. Kỹ năng gom theo nhóm: một phần tử của /sections/skills có category và skills là mảng chuỗi; thêm một kỹ năng thì dùng /sections/skills/0/skills/-.`
	}
	// Nhánh v1: CẮT DÁN NGUYÊN chuỗi đang nằm trong hàm chatSystemPrompt() hiện
	// tại, từ "Bạn là trợ lý chỉnh CV." cho tới hết. Không sửa một ký tự nào —
	// apps/web đang chạy trên đúng prompt đó và test
	// TestChatPromptUsesIntroduceForCVField canh chừng nó.
	return `…`
}
```

Cách làm an toàn cho bước này: đổi tên hàm cũ thành `chatSystemPromptV1()` không tham số, giữ nguyên thân, rồi viết `chatSystemPrompt(v2 bool)` gọi nó ở nhánh `false`. Không có thao tác chép chuỗi thủ công thì không có cơ hội làm sai chuỗi.

Ở handler chat, đổi chỗ gọi:

```go
	prompt := []map[string]string{{"role": "system", "content": chatSystemPrompt(profileIsV2(profileRaw))}, {"role": "user", "content": chatUserPrompt(profileRaw, history, body.Hint, body.Message)}}
```

- [ ] **Step 4: Chạy test**

```bash
cd backend && go test ./... && gofmt -l ./cmd ./internal
```

Kỳ vọng: xanh, không file nào cần format.

- [ ] **Step 5: Commit**

```bash
git add backend/internal/api/server.go backend/internal/api/server_test.go
git commit -m "feat(chat): prompt chọn đường dẫn theo hình dạng hồ sơ

Prompt v1 giữ nguyên từng ký tự — apps/web dùng nó tới SP-5. Prompt v2
dạy model nhắm vào từng gạch đầu dòng thay vì ghi đè cả mảng, vì đó là
điều kiện để màn duyệt diff còn gì đáng duyệt.

Dữ liệu hỏng rơi về nhánh v1 chứ không v2: rơi nhầm nhánh thì model sinh
con trỏ không tồn tại, chốt chặn loại sạch, và người dùng chỉ thấy trợ lý
im lặng không làm được gì.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: SPA đổi mô tả thành danh sách bullet

**Files:**
- Modify: `frontend/apps/web-spa/src/types.ts`
- Modify: `frontend/apps/web-spa/src/components/CVEditorView.tsx`
- Modify: `frontend/apps/web-spa/src/mockData.ts`
- Create: `frontend/apps/web-spa/test/cv-editor-highlights.ui.test.tsx`

**Interfaces:**
- Consumes: hình dạng của `ExperienceItemSchema`, `ProjectItemSchema`, `ActivityItemSchema` (Task 1).
- Produces: `ExperienceItem.highlights: string[]` thay `description: string`, dùng cho SP-3/SP-4.

Spec §2.2(3) gọi đây là **thay đổi giao diện duy nhất chạm vào bản đã duyệt**, và nó bắt buộc: giữ `description` thì mọi đề xuất của AI thành ghi đè nguyên khối.

- [ ] **Step 1: Viết test thất bại**

Tạo `frontend/apps/web-spa/test/cv-editor-highlights.ui.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { CVEditorView } from '../src/components/CVEditorView'
import { mockCVs } from '../src/mockData'

describe('CVEditorView — bullet sửa từng dòng', () => {
  it('hiện mỗi highlight thành một ô nhập riêng', () => {
    render(<CVEditorView cv={mockCVs[0]!} onChange={() => {}} />)
    const inputs = screen.getAllByLabelText(/gạch đầu dòng/i)
    expect(inputs.length).toBeGreaterThanOrEqual(2)
  })

  it('thêm được một dòng mới', () => {
    let updated: unknown = null
    render(<CVEditorView cv={mockCVs[0]!} onChange={(cv) => { updated = cv }} />)
    fireEvent.click(screen.getAllByRole('button', { name: /thêm gạch đầu dòng/i })[0]!)
    expect((updated as { sections: { experience: { highlights: string[] }[] } })
      .sections.experience[0]!.highlights).toHaveLength(
        mockCVs[0]!.sections.experience[0]!.highlights.length + 1,
      )
  })

  it('xoá được một dòng', () => {
    let updated: unknown = null
    render(<CVEditorView cv={mockCVs[0]!} onChange={(cv) => { updated = cv }} />)
    fireEvent.click(screen.getAllByRole('button', { name: /xoá gạch đầu dòng/i })[0]!)
    expect((updated as { sections: { experience: { highlights: string[] }[] } })
      .sections.experience[0]!.highlights).toHaveLength(
        mockCVs[0]!.sections.experience[0]!.highlights.length - 1,
      )
  })
})
```

- [ ] **Step 2: Chạy để thấy nó fail**

```bash
cd frontend && npx vitest run --project ui apps/web-spa/test/cv-editor-highlights.ui.test.tsx
```

Kỳ vọng: FAIL — không tìm thấy ô nhập nào có nhãn "gạch đầu dòng".

Nếu `CVEditorView` nhận props khác `{ cv, onChange }`: đọc file, sửa test cho khớp chữ ký thật **trước khi** viết code.

- [ ] **Step 3: Đổi kiểu**

Trong `frontend/apps/web-spa/src/types.ts`, ở `ExperienceItem`, `ProjectItem` và `ActivityItem`, thay:

```ts
  description: string;
```

bằng:

```ts
  /**
   * Từng gạch đầu dòng là một phần tử riêng, không phải một khối văn bản.
   *
   * Chat sinh JSON Patch nhắm vào /sections/experience/0/highlights/2. Nếu đây
   * là một chuỗi, mọi đề xuất của AI biến thành ghi đè nguyên khối và màn duyệt
   * diff chỉ còn "toàn bộ đoạn cũ" đổi thành "toàn bộ đoạn mới" — không còn gì
   * đáng để người dùng duyệt.
   */
  highlights: string[];
```

- [ ] **Step 4: Sửa `mockData.ts`**

Đổi mọi `description: 'một câu dài'` trong các mục kinh nghiệm, dự án, hoạt động thành `highlights: ['câu một', 'câu hai']`. Tách theo dấu chấm câu của nội dung có sẵn, mỗi mục ít nhất hai dòng để test có cái mà xoá.

- [ ] **Step 5: Sửa `CVEditorView.tsx`**

Thay mỗi `<textarea>` mô tả bằng danh sách sửa từng dòng:

```tsx
<div className="flex flex-col gap-2">
  {item.highlights.map((line, i) => (
    <div key={i} className="flex items-center gap-2">
      <input
        aria-label={`Gạch đầu dòng ${i + 1}`}
        value={line}
        onChange={(e) => onChange(setHighlight(cv, section, index, i, e.target.value))}
        className="flex-1 rounded-md border border-slate-300 px-3 py-2 text-sm"
      />
      <button
        type="button"
        aria-label={`Xoá gạch đầu dòng ${i + 1}`}
        onClick={() => onChange(removeHighlight(cv, section, index, i))}
        className="text-slate-400 hover:text-red-600"
      >
        ×
      </button>
    </div>
  ))}
  <button
    type="button"
    aria-label="Thêm gạch đầu dòng"
    onClick={() => onChange(addHighlight(cv, section, index))}
    className="self-start text-sm font-medium text-indigo-600 hover:text-indigo-700"
  >
    + Thêm gạch đầu dòng
  </button>
</div>
```

Ba hàm thuần đặt cùng file, trên component — chúng là logic sửa dữ liệu, cần đọc được mà không phải đọc JSX:

```tsx
type BulletSection = 'experience' | 'projects' | 'activities'

function mapHighlights(
  cv: CV, section: BulletSection, index: number,
  fn: (lines: string[]) => string[],
): CV {
  const items = cv.sections[section].map((item, i) =>
    i === index ? { ...item, highlights: fn(item.highlights) } : item,
  )
  return { ...cv, sections: { ...cv.sections, [section]: items } }
}

const setHighlight = (cv: CV, s: BulletSection, idx: number, line: number, value: string) =>
  mapHighlights(cv, s, idx, (lines) => lines.map((l, i) => (i === line ? value : l)))

const removeHighlight = (cv: CV, s: BulletSection, idx: number, line: number) =>
  mapHighlights(cv, s, idx, (lines) => lines.filter((_, i) => i !== line))

const addHighlight = (cv: CV, s: BulletSection, idx: number) =>
  mapHighlights(cv, s, idx, (lines) => [...lines, ''])
```

- [ ] **Step 6: Chạy test và typecheck**

```bash
cd frontend && npm run typecheck:spa
npx vitest run --project ui apps/web-spa/test/cv-editor-highlights.ui.test.tsx
```

Kỳ vọng: typecheck xanh (mọi chỗ đọc `description` đều đã đổi), test PASS 3/3.

- [ ] **Step 7: Commit**

```bash
git add frontend/apps/web-spa/src/types.ts frontend/apps/web-spa/src/mockData.ts \
        frontend/apps/web-spa/src/components/CVEditorView.tsx \
        frontend/apps/web-spa/test/cv-editor-highlights.ui.test.tsx
git commit -m "feat(spa): mô tả thành danh sách gạch đầu dòng sửa từng dòng

Thay đổi giao diện duy nhất mà SP-2 chạm vào bản đã duyệt, và nó bắt
buộc: chat sinh patch nhắm vào /sections/experience/0/highlights/2, nên
để nguyên một chuỗi thì mọi đề xuất thành ghi đè nguyên khối và màn duyệt
diff chỉ còn 'toàn bộ đoạn cũ' đổi thành 'toàn bộ đoạn mới'.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Điều kiện hoàn thành SP-2

Đánh dấu xong khi **tất cả** những điều sau đúng:

- [ ] `cd frontend && npm run typecheck` xanh cả ba project.
- [ ] `cd frontend && npx vitest run --project unit --project ui` xanh; số test **tăng**, không giảm.
- [ ] `cd backend && go test ./...` xanh, `gofmt -l ./cmd ./internal` không in gì, `go vet ./...` sạch.
- [ ] `npm run db:backfill-v2` chạy hai lần cho md5 giống hệt nhau (idempotent).
- [ ] `npx tsx ../backend/db/roundtrip-check.ts` báo **24/24 khớp tuyệt đối** khi so với `data` thô. Đây là bằng chứng của đường lùi; **không** chạy `--rollback` lên dữ liệu thật để "chứng minh".
- [ ] Test khứ hồi golden xanh trên cả ba fixture.
- [ ] `curl -sf http://localhost:3000` trả về được — `hr-web` không hề hấn gì.
- [ ] `curl -sf http://localhost:3002/api/health` trả về được — SPA vẫn khoẻ.
- [ ] `git diff --stat main -- frontend/apps/web/` **rỗng**: SP-2 không được chạm vào bản Next.

## Việc SP-2 cố tình chưa làm

- **Chưa ai đọc `data_v2` trong production.** SP-3 là nơi đầu tiên đọc. SP-2 chỉ dựng dữ liệu và chứng minh nó đúng.
- **Chưa xoá `internal/api/compat_ai.go`.** Ba endpoint compat còn phục vụ SPA cho tới khi SP-4 nối chat SSE thật.
- **Chưa viết lại export PDF.** `gofpdf` đổ JSON ra giấy vẫn còn nguyên; thay bằng Playwright render `/print` là việc của SP-5.
- **Chưa viết lại prompt `parse_cv_to_profile`.** Nó sinh hồ sơ v1, và luồng import vẫn là luồng v1 cho tới SP-3.
- **Chưa đụng `packages/matching`, `packages/ai`, `packages/db` bên TypeScript.** Chúng phục vụ `apps/web`; SP-5 gỡ cùng lúc với Next.
- **Tám chỗ đọc hình dạng v1 còn lại trong `matching.go` chưa được sửa.** SP-2 chỉ dạy `profileChunks` đọc v2. Những hàm sau vẫn đọc `p["work"]`, `p["basics"]` ở tầng gốc và sẽ trả rỗng với tài liệu v2: `estimateProfileYears` (:550-575), `allHighlights` (:634-644), `estimatePages` (:657-675), `hasProfileField` (:677-700), khối học vấn trong `richMatchScore` (:501), và hai tiêu chí `"count"` (:718) cùng `"required_fields"` (:768, :774) của `scoreProfileRubric`.

  Cộng lại khoảng **0.45 trọng số** cho điểm 0 mà không báo gì. Chưa có đường nào đưa tài liệu v2 vào worker này nên chưa hỏng thật, nhưng **phải sửa trước khi SP-3 nối dữ liệu thật vào** — sửa 2 trong 8 còn tệ hơn để nguyên cả 8, vì lúc đó điểm sai mà trông như đúng một nửa. Đây là việc riêng của SP-3, ghi ra đây để không ai phải tìm lại.
- **`cv_documents.snapshot_v2` được thêm cột nhưng chưa backfill.** Snapshot gắn với luồng xuất bản, thuộc SP-5.
